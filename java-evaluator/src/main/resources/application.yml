server:
  port: 8081
spring:
  ai:
    openai:
      api-key: ${OPENAI_API_KEY}
      chat:
        options:
          model: gpt-5.6-luna
management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus
